 <div class="container">
   <div class="mt-5 equipment-list">
     <?php foreach ($equipments as $item): ?>
       <div class="equipment-item">
         <h2><?= htmlspecialchars($item->name) ?></h2>
         <p><?= htmlspecialchars($item->description) ?></p>
       </div>
     <?php endforeach; ?>
   </div>
 </div>